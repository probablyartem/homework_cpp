name: build and run 
on: [push]
jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y cmake
      - run: g++ main.cpp my_add.cpp -o main
      - run: mkdir build && cmake -B build -S . && cmake --build build 
      - uses: action/upload-artifactv4
        with:
          name: binaries
          path:
            build/main
            build/tests


  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifactv4
        with:
          name: binaries
      - run: chmod +x tests && ./tests

  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifactv4
        with:
          name: binaries
      - run: chmod +x tests && ./main 3 10
